export enum UserRole {
  ADMIN,
  EMPLOYER,
  CANDIDATE
}

export enum JobType {
    Job,
    Internship
}

export enum salaryType {
    Weekly,
    Monthly,
    Yearly
}

export enum Status {
  Submitted,
  Shortlisted,
  Selected,
  Hired,
  Rejected,
  Terminated
}

